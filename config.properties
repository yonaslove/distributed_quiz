# Distributed System Configuration
# FOR MULTI-NODE SETUP:
# 1. Provide the ACTUAL LAN IP address of each computer (e.g., 192.168.1.50) instead of 'localhost'.
# 2. Ensure each computer has the EXACT SAME config.properties file.
# 3. Firewalls must allow traffic on the ports configured (default 1099, 1100, 1101).
# 
# Format: node.<ID>=<IP_ADDRESS>:<PORT>

node.1=localhost:1099
node.2=localhost:1100
node.3=localhost:1101
